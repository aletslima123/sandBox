import { BetaHighlightDirective } from "./beta-highlight.directive";

describe("BetaHighlightDirective", () => {
  it("should create an instance", () => {
    const directive = new BetaHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
